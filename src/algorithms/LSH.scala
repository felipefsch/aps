package algorithms

import org.apache.spark.rdd.RDD
import utils.Args

object LSH {
  
  /**
   * Input:
   * -rdd: input as (RankID, [Rankings]) tuples
   */
  def filter(rdd: RDD[(Long, Array[Long])]) : Unit = {
    
  }
}